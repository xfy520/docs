import { App } from '@vuepress/core';
import Token from 'markdown-it/lib/token';
declare const _default: (token: Token, info: string, code: string, preStyleList: Array<string>, app: App) => [
    number,
    string
] | null;
export default _default;
